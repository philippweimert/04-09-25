# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added
- Initial project setup after refactoring.

### Fixed
- Downgraded `react` and `react-dom` to `^18.2.0` to resolve peer dependency conflicts.
- Downgraded `date-fns` to `^3.6.0` to resolve peer dependency conflicts with `react-day-picker`.
